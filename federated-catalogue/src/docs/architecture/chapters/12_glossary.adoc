:jbake-title: Glossary
:jbake-type: page_toc
:jbake-status: published
:jbake-menu: arc42
:jbake-order: 12
:filename: /chapters/12_glossary.adoc
ifndef::imagesdir[:imagesdir: ../../images]

:toc:

[[section-glossary]]
== Glossary

[cols="e,2e" options="header"]
|===
|Term |Definition

|Credential (formerly Self-Description / SD)
|A credential is a Verifiable Presentation (VP) or standalone Verifiable Credential (VC) that expresses characteristics of a Resource, Service Offering or Participant and describes properties and Claims which are linked to the Identifier. The catalogue accepts both forms; in strict/Gaia-X mode a VP wrapper is required. In the Gaia-X specificationfootnote:[https://docs.gaia-x.eu/technical-committee/identity-credential-access-management/24.07/credential_format/[Gaia-X Identity, Credential and Access Management / Credential format]], this concept replaces what was previously called a "Self-Description". At the catalogue's API and storage level, credentials are managed as **Assets**.

|Verifiable Presentation (VP)
|A verifiable presentation expresses data from one or more verifiable credentials, and is packaged in such a way that the authorship of the data is verifiable. 

|Verifiable Credential (VC)
|A credential is a set of one or more claims made by the same entity. A verifiable credential is a set of tamper-evident claims and metadata that cryptographically prove who issued it. 

|Claim
|An assertion made about a subject within Gaia-X.

|Proof
|To make the VP tamper-resistant, VPs (and VCs) are signed. The proof object contains the information to check if the presented data was manipulated. The object contains, besides others, the signed hash of the data, the link to therespective public key, and information regarding the algrithm used for signing.

|Issuer
|A role an entity can perform by asserting claims about one or more subjects and creating a verifiable credential from these claims.

|Schema
|Generic term for either an ontology, shape graph, controlled vocabulary (all RDF-based), or a non-RDF schema (JSON Schema, XML Schema). RDF schemas participate in the verification pipeline and composite schema graph. Non-RDF schemas are stored as reference files and do not participate in automatic SHACL verification.

|Term
|A node in a RDF graph. A node can be an IRI, a literal, or a blank node.

|DID
|A portable URL-based identifier, also known as a DID, associated with an entity. An example of a DID is `did:web:example.com`. 

|DID Document
|The DID presented in an VC or proof links to an DID-Document. The DID-Document is a description of an identity. It is stored dezentralized by the owner. The DID-Document provides cryptographic information that can be used to verify any document. Thereby, it can be ensured that the document is trusted.

|Asset
|Generic term for any content stored in the catalogue — including RDF-based credentials and non-RDF content (PDFs, JSON, YAML, images). At the API level, the unified `/assets` endpoint handles both types. Every asset is identified by an IRI (see ADR 7). See ADR 4.

|IRI (Internationalized Resource Identifier)
|A unique identifier for an asset in the catalogue. Supported formats include DIDs (e.g., `did:web:example.com`), UUID URNs (e.g., `urn:uuid:550e8400-...`), generic URNs, and HTTP/HTTPS IRIs. For RDF assets, the IRI is extracted from the content; for non-RDF assets, a UUID URN is generated per RFC 4122. See ADR 7.

|id/credentialSubject of an Entity
|Unique identifier for entities stored in the catalogue.
For RDF credentials, this is the `credentialSubject.id` (typically a DID), which must be present as an attribute in the claims.
For non-RDF assets, this is a UUID URN generated by `IriGenerator`.
The identifier must be a valid IRI (see ADR 7).

|Loire
|The Gaia-X v2 trust framework generation, corresponding to ICAM 24.07. Loire credentials use the `https://w3id.org/gaia-x/2511#` namespace, W3C VC 2.0 data model, and JWT envelopes with VC-JOSE-COSE headers. Replaces the Tagus generation. See ADR 8, ADR 9.

|Tagus
|The Gaia-X v1 trust framework generation, corresponding to ICAM 22.10. Tagus credentials used the `gax-core:` / `gax-trust-framework:` namespaces, W3C VC 1.1 data model, and JSON-LD with data-integrity proofs (JsonWebSignature2020). **No longer supported** — the catalogue rejects VC 1.1 context URIs and Linked Data proofs. Credentials must be re-issued in Loire/VC 2.0 format. See ADR 9.

|ICAM 24.07
|Gaia-X Identity, Credential and Access Management specification version 24.07. Defines the Loire credential format, including JWT payload structure (credential as top-level payload, no `vc`/`vp` claim wrapper), `EnvelopedVerifiableCredential` for VP embedding, and mandatory trust anchor chain validation.

|W3C VC-JOSE-COSE
|W3C Recommendation for securing Verifiable Credentials using JOSE and COSE. Defines the IANA-registered JWT header values (`vc+jwt`, `vp+jwt` for `typ`; `vc`, `vp` for `cty`) and HTTP media types (`application/vc+jwt`, `application/vp+jwt`). The canonical standard for JWT-wrapped credentials in the catalogue (see ADR 8).

|EnvelopedVerifiableCredential
|A W3C VC 2.0 / ICAM 24.07 mechanism for embedding inner Verifiable Credentials inside a VP JWT. Each inner VC is wrapped as a JSON object with `type: "EnvelopedVerifiableCredential"` and an `id` field containing a `data:` URL (`data:application/vc+jwt,eyJ...`) from which the compact JWT is extracted.

|Machine-Readable Asset
|An asset variant intended for automated processing — typically an RDF credential (JSON-LD), JSON, or XML file. When linked to a human-readable asset, its `asset_type` column is set to `MACHINE_READABLE` and `linked_asset_id` points to the corresponding human-readable asset. Surfaced in API responses via the `humanReadableId` field.

|Human-Readable Asset
|An asset variant intended for human consumption — typically a PDF, DOCX, HTML, or plain-text file. Uploaded via `POST /assets/{id}/human-readable`. When linked, `asset_type` is `HUMAN_READABLE` and `linked_asset_id` points to the corresponding machine-readable asset. Surfaced in API responses via the `machineReadableId` field.

|Asset Link
|A bidirectional association between a machine-readable asset and a human-readable asset. Stored as two columns on the `assets` table (`asset_type` and `linked_asset_id` FK). Mirrored in the graph DB as `fcmeta:hasHumanReadable` and `fcmeta:hasMachineReadable` RDF triples for semantic queryability.

|AssetType
|An enum distinguishing asset variants: `MACHINE_READABLE`, `HUMAN_READABLE`, `GENERIC` (no link). Stored in the `asset_type` column on the `assets` table.

|fcmeta:hasHumanReadable
|RDF property in the protected `fcmeta:` namespace (`https://projects.eclipse.org/projects/technology.xfsc/federated-catalogue/meta#`) written to the graph DB when an asset link is created. Points from a machine-readable asset's IRI to its human-readable counterpart's IRI.

|fcmeta:hasMachineReadable
|RDF property in the protected `fcmeta:` namespace written to the graph DB when an asset link is created. Points from a human-readable asset's IRI to its machine-readable counterpart's IRI.

|===
